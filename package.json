{
  "name": "trending-content-generator",
  "version": "1.0.0",
  "description": "自动生成每小时热门内容文案",
  "main": "scripts/generate-content.js",
  "scripts": {
    "generate": "node scripts/generate-content.js",
    "update-index": "node scripts/update-index.js",
    "build": "node scripts/build.js",
    "dev": "npx serve dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
